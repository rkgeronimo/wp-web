{% block content %}
    <div class="wrap">
        <form method="post">
            <h1 id="add-new-inventory" class="wp-heading-inline">{{ post.post_title }}</h1>
            <table class="form-table">
                <tr>
                    <th>
                        Datum ispita
                    </th>
                    <td>
                        <input name="exam" type="date" value="{{ meta.exam }}" />
                    </td>
                </tr>
                <tr>
                    <th>
                        Asistent
                    </th>
                    <td>
                        <select class="preSignupSelect" id="pr1" style="width: 70%" name="assistant">
                            <option value="" {{ meta.assistant ?: 'selected' }}></option>
                            {% for userItem in userList %}
                                <option value="{{ userItem.ID }}" {{ meta.assistant == userItem.ID ? 'selected="selected"' }}>{{ userItem.data.display_name }}</option>
                            {% endfor %}
                        </select>
                    </td>
                </tr>
                <tr>
                    <th>
                        Delegat
                    </th>
                    <td>
                        <input name="delegate" type="text" value="{{ meta.delegate }}" />
                    </td>
                </tr>
                <tr>
                    <th>
                        Tečaj je zaključan?
                    </th>
                    <td>
                        <input name="locked" type="checkbox" {{ meta.locked ? 'checked' }} />
                    </td>
                </tr>
                <tr class="links-table">
                    <td>{{ sendEmails }}</td>
                </tr>
                <tr><td></td></tr>
                
                <tr class="links-table">
                    <td>{{ allMedical }}</td>
                    <td>{{ allLiability }}</td>
                    <td>{{ allBrevet }}</td>
                    <td><b>{{ hrsReport }}</b></td>
                </tr>
                
            </table>
            {{ table.display }}
        <input type="submit" value="spremi">
        </form>
    </div>

    <div id="rkg-unregister-dialog" style="display: none; position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); background: white; border: 1px solid #ccc; padding: 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); z-index: 1000; min-width: 300px;">
        <h3>Potvrda brisanja</h3>
        <p>Jeste li sigurni da želite odjaviti <strong id="rkg-unregister-user-name"></strong> s ovog tečaja?</p>
        <p>Ova akcija se ne može poništiti.</p>
        <div style="text-align: right; margin-top: 20px;">
            <button type="button" id="rkg-cancel-btn" class="button" style="margin-right: 10px;">Odustani</button>
            <button type="button" id="rkg-confirm-btn" class="button button-primary" style="background-color: #dc3232; border-color: #dc3232;">Da, odjavi</button>
        </div>
    </div>
    <div id="rkg-dialog-overlay" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 999;"></div>
{% endblock %}
