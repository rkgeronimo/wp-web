# Python dependencies for WordPress backup script

## NOTE: Intended for production use only!
## Don't need to run this for the development env,
## unless you're testing the very script.

# Core dependencies
python-dotenv==1.0.0       # Environment variable management
dropbox==12.0.2            # Dropbox API client

# Future provider support (commented out for now)
# boto3==1.34.0            # AWS S3 support
# google-cloud-storage==2.14.0  # Google Cloud Storage support
